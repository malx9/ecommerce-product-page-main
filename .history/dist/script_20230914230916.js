"use strict";

const menuBtn = document.getElementById(menuButton);
const closeBtn = document.getElementById(closeButton);
const overlay = document.getElementById(overlay);

menuBtn.addEventListener(click, () => console.log("menuBtn CLICK"));
